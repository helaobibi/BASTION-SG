# Tinkr 残留扫描结果

> 已清理：15处无用声明、AuraTable.lua (Tinkr.era x4)、Unit.lua (Tinkr.classic/era x3)、UnitManager.lua (Tinkr.Util.ObjectManager)、MythicPlusUtils 模块、dumpspells 命令、宁神射击功能

---

## 一、仅剩 2 处 Tinkr 引用

| 文件 | 行号 | 代码 | 状态 |
|------|------|------|------|
| **init.lua** | 4 | `Bastion._Tinkr = Tinkr` | 根入口，等 Unit.lua 清理后一起删 |
| **Unit.lua** | 1, 1075 | `local Tinkr` + `Tinkr.Common.GetAnglesBetweenPositions(...)` | 唯一真正使用处，可用 `math.atan2` 替代 |

---

## 二、Tinkr 全局函数（运行环境提供，非 `Tinkr.*` 调用，无需清理）

这些函数由运行环境注入为全局函数，代码中直接调用，不经过 `Tinkr.` 前缀：

### 对象系统
| 函数 | 用途 | 使用文件 |
|------|------|---------|
| `Object()` | 获取对象指针 | Unit, UnitManager, Spell, Item |
| `ObjectGUID()` | 获取对象GUID | UnitManager, ObjectManager, Unit |
| `ObjectPosition()` | 获取3D坐标 | Unit, Item |
| `ObjectRotation()` | 获取朝向角度 | Unit |
| `ObjectHeight()` | 获取对象高度 | Unit |
| `ObjectCombatReach()` | 获取战斗范围 | Unit |
| `ObjectID()` | 获取NPC/对象ID | Unit |
| `ObjectType()` | 获取对象类型 | ObjectManager |
| `ObjectMovementFlag()` | 获取移动标志 | Unit |
| `Objects()` | 枚举所有对象 | ObjectManager |

### 交互/移动
| 函数 | 用途 | 使用文件 |
|------|------|---------|
| `FaceObject()` | 面向目标 | BM/SV Module |
| `MoveToRaw()` | 移动到坐标 | Unit |
| `RotateVector()` | 旋转向量计算 | Unit |
| `Click()` | 点击地面放置技能 | Spell, Item |
| `TraceLine()` | 视线检测 | Unit, UnitManager |
| `GetUnitAttachmentPosition()` | 获取单位附着点坐标 | Unit |

### 施法/输入
| 函数 | 用途 | 使用文件 |
|------|------|---------|
| `IsSpellPending()` | 检查技能等待状态 | Spell, Item |
| `SpellStopCasting()` | 打断施法 | BM/SV Module |
| `MouselookStop/Start()` | 鼠标视角控制 | Spell, Item |
| `GetKeyState()` | 检测键盘按键 | BM/SV Module |

---

## 三、下一步

将 Unit.lua:1075 的 `Tinkr.Common.GetAnglesBetweenPositions` 替换为 `math.atan2`，然后删除 Unit.lua 的 `local Tinkr` 和 init.lua 的 `Bastion._Tinkr = Tinkr`，即可彻底移除 Tinkr。